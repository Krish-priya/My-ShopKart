{
  "name": "my-shopkart",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}