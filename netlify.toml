# Build from repo root → output in client/dist
[build]
  command = "npm run build"
  publish = "client/dist"

# React Router SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
